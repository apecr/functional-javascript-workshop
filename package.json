{
  "name": "functional-javascript-workshop",
  "version": "1.0.0",
  "description": "https://github.com/timoxley/functional-javascript-workshop",
  "main": "helloworld.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "deps": "npm install",
    "test": "./node_modules/istanbul/lib/cli.js cover ./node_modules/.bin/_mocha",
    "coveralls": "cat ./coverage/lcov.info | ./node_modules/.bin/coveralls",
    "coverage": "node_modules/.bin/istanbul cover --report lcovonly --report html --dir test-reports ./node_modules/.bin/_mocha",
    "sonar": "sonar-scanner"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.1.2",
    "coveralls": "^3.0.1",
    "istanbul": "^0.4.5",
    "mocha": "^5.1.1"
  }
}
